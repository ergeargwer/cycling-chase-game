# Retro pixel asset generator

```bash
# from repo root
python3 tools/retro_gen/generate_all.py
```

Writes to `assets/retro/` per `docs/retro-scene-props-spec.md` (v2) and `docs/retro-asset-spec.md`.

Requires: `Pillow`
