# Retro assets (paths locked to design specs)

See:
- `docs/retro-asset-spec.md` — characters / chasers / hud / fx
- `docs/retro-scene-props-spec.md` — tiles / mid / props / parallax layers

Until PNGs are present, ChaseScene falls back to modern Graphics scenery
while still applying nearest-neighbor scaling + CRT scanlines for characters.
